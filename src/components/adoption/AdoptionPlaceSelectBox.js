import React from 'react';
import styled from 'styled-components';

import { Grid } from '../../elements';

const AdoptionPlaceSelectBox = (props) => {
  return (
    <Grid>
      <SelectBox></SelectBox>
    </Grid>
  );
};

const SelectBox = styled.section``;

export default AdoptionPlaceSelectBox;
